---
import Layout from '../layouts/BaseLayout.astro';

const allBlogPosts = await Astro.glob('./blog/*.md')
---

<Layout title='Blog'>
    <h1>Willkommen auf meinem Blog</h1>
    <p>Hier schreibe ich über Themen die mich interessieren.</p>
    <ul>
        {allBlogPosts.map(post =>
        <li>
            <a href={post.url}>
                {post.frontmatter.title}
            </a>
        </li>
        )}
    </ul>
</Layout>